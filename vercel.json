{
  "version": 2,
  "framework": null,
  "buildCommand": "npm install && npx prisma generate --schema backend/prisma/schema.prisma && npm install --prefix frontend --include=dev && npm run build --prefix frontend",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/index.ts": {
      "maxDuration": 30,
      "memory": 1024,
      "includeFiles": "backend/node_modules/.prisma/**"
    }
  },
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index.ts" },
    { "source": "/((?!api/).*)", "destination": "/index.html" }
  ]
}
